/*#include <iostream>
#include <string>
#include <stdio.h>
#include <algorithm>
using namespace std;


int main() {
    int n;
    cin >> n;
    int* solder = new int[n * n];
    for (int i = 0; i < n * n; ++i) {
        cin >> n;
    }

    sort(solder, solder + (n * n));

    int** res_solder = new int* [n];
    int level = 1;
    for (int i = 0; i < n; ++i) {
        res_solder[i] = new int[n];
        for (int i = 0; i < n; ++i) {
            

        }
    }
    return 0;
}*/